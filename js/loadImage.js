var slider = new DiaShow();
createDiaShow({
  diaShow:slider,
  reverseDirection:false,
  delay:10000
});
/*
this.createDiaShow = function(id, delay, direction){
  window.onload = function(){
    var slider = new DiaShow("#diaShow");
    slider.setDelay(5000);
    slider.reverseDirection(true);
    slider.start();
  };
}
*/
